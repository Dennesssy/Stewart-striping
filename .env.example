# Copy this file to `.env.local` or set these variables in your deployment platform.
# Provide your own database connection string (Postgres recommended for production).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"

# Generate a long random string. For local dev you can use `openssl rand -base64 32`.
NEXTAUTH_SECRET="replace-with-strong-secret"
